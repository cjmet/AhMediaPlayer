<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="MauiMediaPlayer.MainPage"
             >

    <ScrollView>
        <VerticalStackLayout
            Padding="20,0"
            Spacing="0">
            <Image
                Source="angel_hornet_logo.png"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="Angel Hornet Logo.  A tuxedo cat with, angel wings, and a stinger" />

            <toolkit:MediaElement x:Name="mediaElement" Source="embed://angel_hornet_logo.png"
              ShouldShowPlaybackControls="True" />

            <!--<Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />-->

            <!--<Label
                Text="Test Text Before the Lists"
                Style="{StaticResource NormalText}"
                 />-->

            <ListView x:Name="TestPlaylist" MaximumHeightRequest="40">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding LineItem}"  Detail="{Binding Description}" TextColor="Black"/>
                        <!--<TextCell Text="{Binding LineItem}"  TextColor="Black"/>-->
                        <!--<ViewCell>
                            <StackLayout Orientation="Horizontal" >
                                <Label Text="{Binding Id}" TextColor="Black"/>
                                <Label Text=" - " />
                                <Label Text="{Binding Name}" TextColor="Black"/>
                                <Label Text=" - " />
                                <Label Text="{Binding Description}" TextColor="Black"/>
                            </StackLayout>
                        </ViewCell>-->
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Label 
                Text=""
                Style="{StaticResource NormalText}"
                 />

            <ListView x:Name="TestSonglist" MaximumHeightRequest="280">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Title}"  Detail="{Binding Comment}" TextColor="Black"/>
                        <!--<TextCell Text="{Binding LineItem}"  TextColor="Black"/>
                        <ViewCell>
                            <StackLayout Orientation="Horizontal" >
                                <Label Text="{Binding Id}" TextColor="Black"/>
                                <Label Text=" - " />
                                <Label Text="{Binding Title}" TextColor="Black"/>
                                <Label Text=" - " />
                                <Label Text="{Binding Comment}" TextColor="Black"/>
                            </StackLayout>
                        </ViewCell>-->
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!--<Label
                Text="Test Text After the Lists"
                Style="{StaticResource NormalText}"
                 />-->

            <!--<Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource NormalText}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />-->

            <!--<Button
                x:Name="CounterBtn"
                Text="Click me" 
                SemanticProperties.Hint="Counts the number of times you click"
                Clicked="OnCounterClicked"
            HorizontalOptions="Center" />-->

            <StackLayout Orientation="Horizontal"  Margin="5" HorizontalOptions="FillAndExpand">

                <Label x:Name="pathText" 
                  Text="Path" 
                  Style="{StaticResource NormalText}"
                  LineBreakMode="MiddleTruncation"
                  HorizontalOptions="StartAndExpand"
                   />


                <Label x:Name="counterText" 
                    Text="Counter" 
                    Style="{StaticResource NormalText}"
                    HorizontalOptions="EndAndExpand"
                     />

            </StackLayout>









        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
